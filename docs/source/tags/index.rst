tags commands
=============

The ``tags`` commands inspect the tags configured on a device. Tags are
labels attached to devices that job definitions can require, so that a job is
only scheduled onto a device carrying the matching tag.

These commands are **read-only**: they list and show tags but do not create
or modify them.

.. toctree::
   :maxdepth: 1

   list
   show
