This is the documentation repository for Linaro's LAVA CLI (``lava-cli``).

The LAVA CLI is a command line tool for interacting with Linaro's LAVA
Gateway REST API using subscription-based Biscuit token authorisation.

To build locally, go into ``docs`` and run ``make html``.
